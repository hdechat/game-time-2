var Block = require('./Block.js');

class Logs extends Block{
  // constructor(x, y, width, height) {
  //   super(x, y, width, height);
  // }

  // moveLeft() {
  //   this.x -= speed //pixel speed
  // }
}

module.exports = Logs;